      </Resources>
      <TerminalServers>
         <TerminalServer ID="${hostname}" Name="${hostname}" LastUpdated="${currentdatetime}" />
      </TerminalServers>
   </Publisher>
</ResourceCollection>